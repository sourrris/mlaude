# Mlaude Evals

These scenarios are the local harness baseline for the vNext research loop.

Track at minimum:

- `pass@1`
- `citation_coverage`
- `step_completion_rate`
- `insufficient_evidence_precision`

Each scenario in `scenarios/research-harness.json` is designed to be runnable
against the local FastAPI + Ollama stack without cloud dependencies.
